import { connect } from 'dva';
import { Flex } from 'antd-mobile';


function ClassesPage() {
  return (
   <Flex>ClassesPage</Flex>
  )}

const mapDispatch2Props = (dispatch) => ({

})
export default connect(()=>({}), mapDispatch2Props)(ClassesPage);
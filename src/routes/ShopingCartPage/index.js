import { connect } from 'dva';
import { Flex } from 'antd-mobile';


function ShopingCartPage() {
  return (
   <Flex>ShopingCartPage</Flex>
  )}

const mapDispatch2Props = (dispatch) => ({

})
export default connect(()=>({}), mapDispatch2Props)(ShopingCartPage);